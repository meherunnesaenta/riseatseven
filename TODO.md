# TODO - Rise at Seven Clone UI sync

## Step 1
- Update `src/index.css` with Rise-at-Seven theme variables (colors, surfaces, radii) and better global typography/spacing. ✅ (already present)

## Step 2
- Sync `Navbar.jsx` inline CSS to use the new `src/index.css` variables where possible (mint/black, radii, transition speeds). 

## Step 3
- Sync `Footer.jsx` inline CSS to use the new `src/index.css` variables where possible.

## Step 4
- Run `npm run dev` and visually verify: navbar blur/background, footer dark panel styling, hero section background alignment.

## Step 5
- Run `npm run build` to ensure no build/lint issues.

